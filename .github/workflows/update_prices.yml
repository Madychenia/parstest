name: Hourly Price Check

on:
  schedule:
    - cron: '0 * * * *'  # Ровно в 00 минут каждого часа
  workflow_dispatch:      # Чтобы можно было запустить кнопкой вручную

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install pandas requests beautifulsoup4 pytz

      - name: Run parser
        run: python app.py --parse

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add price_history.json last_run.json
          git commit -m "Hourly price update" || echo "No changes to commit"
          git push
